======
escaped-simple_escaped
======

\%

---

(document
  (main

      (escaped
        (escaped_char)
      )

  ) 
)

; This test causes the testing function to not halt
;
; ======
; escaped-multiple_escaped
; ======
; 
; \#\$\%\&\^\_\{\}\|\~\\
; 
; ---
; 
; (document
;  (main
;       (escaped(escaped_char))
;       (escaped(escaped_char))
;       (escaped(escaped_char))
;       (escaped(escaped_char))
;       (escaped(escaped_char))
;       (escaped(escaped_char))
;       (escaped(escaped_char))
;       (escaped(escaped_char))
;       (escaped(escaped_char))
;       (escaped(escaped_char))
;       (escaped(escaped_char))
;   )
; )
