======
escaped-simple_escaped
======

\%

---

(document
  (main

      (escaped
        (escaped_char)
      )

  ) 
)

======
escaped-multiple_escaped
======

\#\$\%\&\^\_\{\}\|\~\\

---

(document
  (main
      (escaped(escaped_char))
      (escaped(escaped_char))
      (escaped(escaped_char))
      (escaped(escaped_char))
      (escaped(escaped_char))
      (escaped(escaped_char))
      (escaped(escaped_char))
      (escaped(escaped_char))
      (escaped(escaped_char))
      (escaped(escaped_char))
      (escaped(escaped_char))
  ) 
)
